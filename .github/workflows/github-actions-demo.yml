name: GitHub Actions Demo
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    container:
     image: lheiz/dc-armv7-nymea-d10:latest
    steps:  
      - uses: actions/checkout@v2
      - run: dpkg-buildpackage -b -aarmhf

