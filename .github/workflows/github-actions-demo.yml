name: GitHub Actions Demo
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:  
    - uses: docker://lheiz/dc-armv7-nymea-d10:latest
    - uses: actions/checkout@v2
    - name: What OS is running
      run: uname -a
    - name: List files
      run: ls

